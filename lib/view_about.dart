import 'package:flutter/material.dart';

class AboutMainView extends StatelessWidget {
  const AboutMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("About View");
  }
}
