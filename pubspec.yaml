name: mcinfo_app
description: "Display some helpful information from Minecraft APIs"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  adwaita: ^1.1.0
  dart_minecraft: ^0.3.4
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.7.0
  glowstone: ^0.0.2
  gradient_icon: ^2.0.9
  http_requests: ^3.2.0
  ionicons: ^0.2.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
